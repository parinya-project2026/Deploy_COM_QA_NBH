# ==============================================
# QA Hospital System - Environment Variables
# ==============================================

# -----------------------------------------------
# สำหรับ Development (Local PostgreSQL)
# -----------------------------------------------
# DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/qa_hospital?schema=public"

# -----------------------------------------------
# สำหรับ Vercel Postgres (Production)
# -----------------------------------------------
# ตัวแปรเหล่านี้จะถูกตั้งค่าอัตโนมัติเมื่อเชื่อมต่อ Vercel Postgres
POSTGRES_PRISMA_URL="postgresql://..."
POSTGRES_URL_NON_POOLING="postgresql://..."

# -----------------------------------------------
# LINE Notification (Optional)
# -----------------------------------------------
# LINE_CHANNEL_ACCESS_TOKEN="your_line_channel_access_token"
